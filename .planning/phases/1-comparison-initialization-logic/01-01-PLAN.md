---
wave: 1
depends_on: []
files_modified:
  - hooks/useCollections.ts
  - components/CollectionExplorer.tsx
  - components/CollectionContextMenu.tsx
  - app/page.tsx
autonomous: true
---

# Phase 1: Comparison Initialization Logic - Plan 1

## Goal
Integrate context menu "Compare" action and modify state to track comparison targets.

## Tasks

### 1. Update State Management handling Comparison
<task>
<read_first>
- hooks/useCollections.ts
</read_first>
<action>
1. Modify `useCollections.ts` state. Export `compareTargetId` (`string | null`) and `setCompareTargetId`.
2. Add these to the return signature of the `useCollections` hook.
</action>
<acceptance_criteria>
- `grep -n "compareTargetId" hooks/useCollections.ts` returns > 0 lines.
- `grep -n "setCompareTargetId" hooks/useCollections.ts` returns > 0 lines.
</acceptance_criteria>
</task>

### 2. Connect the Right-Click Context Menu
<task>
<read_first>
- components/CollectionContextMenu.tsx
- components/CollectionExplorer.tsx
</read_first>
<action>
1. Update `CollectionContextMenu.tsx` to display a "Karşılaştır" (Compare) context menu item. Limit this visibility to files with type='file'.
2. In `CollectionContextMenu.tsx`, add an `onCompare(id: string)` prop. Call it when "Karşılaştır" is clicked.
3. In `CollectionExplorer.tsx`, add an `onCompare` prop, and pass it to `CollectionContextMenu`.
</action>
<acceptance_criteria>
- `grep -n "Karşılaştır" components/CollectionContextMenu.tsx` returns > 0 lines.
- `grep -n "onCompare" components/CollectionExplorer.tsx` returns > 0 lines.
</acceptance_criteria>
</task>

### 3. Expose Compare Action in the main router
<task>
<read_first>
- app/page.tsx
</read_first>
<action>
1. Inside `app/page.tsx`, import `compareTargetId` and `setCompareTargetId` from `useCollections()`.
2. Add a `handleCompare` callback that takes `(id: string)` and invokes `setCompareTargetId(id)`.
3. Pass `onCompare={handleCompare}` down to the `CollectionExplorer` component.
4. Add `compareTargetFile` derivation (using `findItemById`) and pass it down alongside the context.
</action>
<acceptance_criteria>
- `grep -n "handleCompare" app/page.tsx` returns > 0 lines.
- `grep -n "compareTargetId" app/page.tsx` returns > 0 lines.
</acceptance_criteria>
</task>

## Verification
<must_haves>
- The context menu inside the file explorer properly displays a "Karşılaştır" option for files.
- Selecting it updates the `compareTargetId` state successfully.
</must_haves>
